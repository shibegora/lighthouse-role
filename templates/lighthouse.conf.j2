server {
    listen 80;
    server_name {{ lighthouse_domain | default('localhost') }};

    root /var/www/lighthouse;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }
}